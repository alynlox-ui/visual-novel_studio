# 🎬 Galgame 制作工坊 · 更新报告

**更新日期**：2026.08.21  
**当前版本**：v1.1.0

亲爱的创作者们，本次更新带来三个重磅能力：**CG 动画 / 视频**、**更聪明的 AI 辅助**，以及**对话与立绘的实时联动**。现在，你可以让故事更像一部会动的视觉小说了。

---

## ✨ 新增功能

### 1. 🎥 CG 动画 / 视频导入
- 每个场景都可以添加一段 CG 动画或视频。
- 支持直接上传本地视频，也可以粘贴视频 URL。
- 进入该场景时，CG 动画会自动循环播放，并默认静音以确保浏览器自动播放可用。
- 视频会显示在背景与角色立绘之间，不遮挡对话文本框和选项。

### 2. 🤖 AI 辅助全面升级：每一种用途，独立 API 接口
- 现在支持 **图像生成、视频生成、文本生成** 三大 AI 能力。
- 每种用途都有独立的 API 配置：Base URL、API Key、模型、路径，互不干扰。
- 图像生成可一键应用到：当前场景背景、场景角色立绘、人物库基础立绘、人物库动作图。
- 视频生成可一键应用到：当前场景 CG 动画。
- 文本生成可一键按行拆分为多句对话，追加到当前场景。
- API Key 仅保存在你自己的浏览器本地，我们不会上传到任何服务器。

### 3. 🎭 对话与立绘联动
- 每句台词都可以指定「演出角色」和「动作 / 表情」。
- 不指定演出角色时，系统会自动用“说话人”匹配场景里的角色立绘。
- 播放到某句台词时：
  - 当前说话角色的立绘会高亮。
  - 其他角色立绘自动压暗，让注意力聚焦在说话人身上。
  - 立绘会随台词自动切换动作 / 表情。
  - 切换过程带有平滑淡入淡出效果。

### 4. 📝 多组对话时间轴
- 一个场景不再只能写一段文本。
- 现在可以为每个场景添加多组对话，并自由排序、编辑说话人和台词。
- 旧版“单条文本”仍然兼容，可一键转换为多组对话。

### 5. 💾 草稿自动保存
- 编辑内容会自动保存到浏览器本地。
- 关闭或刷新页面后，再次打开会自动恢复上一次编辑的项目。
- 顶部新增「保存草稿」按钮和保存状态提示，进度不再丢失。

---

## 🛠 优化与修复
- 修复 Render 免费实例休眠后首次打开长时间无响应的问题：新增健康检查接口，访问更稳定。
- 优化快进（已读模式）在多组对话场景下的停止逻辑。
- 修复语速为“瞬间”时，最后一句对话结束后不显示选项的问题。
- 修复变量插入按钮可能插入到错误输入框的问题。
- 修复角色动作动画（呼吸 / 弹跳 / 抖动）不生效的问题。
- 编辑器草稿保存改为防抖自动保存，连续输入更流畅。
- 支持按 Esc 关闭 AI / 数值 / 人物 / UI 设置弹窗。
- 编辑器新增「🎨 UI 设置」面板：提供粉白默认、深色夜晚、复古纸张、极简黑白 4 套预设，可实时预览并微调文本框、角色名牌、选项区、点击提示。
- 编辑器新增「📋 批量导入台词」：按“说话人：台词”格式粘贴脚本，可一键追加或替换当前场景对话。
- 新增「▶ 测试当前场景」：可直接从当前选中的场景开始试玩，不用每次从头跑剧情。
- 左侧场景列表底部新增剧本统计：场景数、台词数、字数，悬停可查看选项 / 结局 / Flag / CG 视频数量。
- 新增「📖 大纲 / 分支表」：在动工前先写一句话简介、故事摘要、路线规划，并用计划分支表梳理各路线触发条件与目标；同时自动汇总当前所有场景的分支总览。
- 流程图画布支持右键场景方块快速连线，也支持从节点右侧的灰/黄/蓝圆点拖拽到目标场景，直接生成默认下一场景、自动分支或选项分支。
- 场景新增「🎵 背景音乐 (BGM)」：可上传或粘贴音频 URL，进入场景自动循环播放，支持音量调节。
- 每句台词新增「配音」输入框：可填写配音音频 URL，播放到该句时自动播放语音。
- 新增 Render 部署健康检查路径 `/healthz`。

---

## 📌 使用小贴士
1. **想做 CG 动画**：在场景右侧「🎥 CG 动画 / 视频」中上传或填写视频地址。
2. **想用 AI 生图 / 生视频 / 写台词**：点击顶部「🤖 AI 辅助」，分别填写对应 API 配置后即可使用。
3. **想让立绘跟着台词“演”起来**：在场景中添加角色立绘，然后在每句对话里指定演出角色和动作。
4. **怕丢进度**：随时点「💾 保存草稿」，或使用「导出 JSON」永久备份项目。

---

> 本次更新已同步至线上版：https://galgame-studio.onrender.com  
> 本地版和 Render 部署包也已同步更新。

感谢每一位创作者的支持，期待你用 Galgame 制作工坊做出更动人的故事 💕
